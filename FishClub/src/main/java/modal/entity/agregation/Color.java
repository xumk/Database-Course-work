package modal.entity.agregation;

/**
 * Enum для цвета наживки, но нигде не используется
 * не надо его пихать в отчет
 */
public enum Color {
    RED,
    BLUE,
    BLACK,
    WHITE;

    private Color() {
    }
}